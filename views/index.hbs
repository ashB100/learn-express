<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
    </head>
    <body>
        <h1>Handlebars</h1>
        {{#each users as |user|}}
        <div>
            <img src="{{user.username}}_sm.jpg" alt="">
            <a href="/{{user.username}}">{{user.name.full}}</a></li>
        </div>
        {{/each}}
    </body>
</html>